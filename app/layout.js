export const metadata = {
  title: 'IB Dock Management',
  description: 'Inbound Dock Management Dashboard',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
